import { TrimTextDirective } from './trim-text.directive';

describe('TrimTextDirective', () => {
  it('should create an instance', () => {
    const directive = new TrimTextDirective();
    expect(directive).toBeTruthy();
  });
});
